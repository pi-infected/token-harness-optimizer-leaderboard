The Python project in the current directory has failing tests.

Run the test suite (`python3 -m unittest discover -s tests -t .`), find the
root cause, and fix the bug in the library code. Do NOT modify anything under
`tests/` — the tests are the specification. When you are done, the full suite
must pass.

In your final reply, state in one or two sentences what the bug was.
