The `ledger/` library has several independent bugs — multiple tests in
the suite are currently failing.

Run the test suite with:

    python3 -m unittest discover -s tests -t .

Fix the source code in `ledger/` until the entire suite passes. Do NOT
modify or delete any file under `tests/`. Re-run the full suite after
each change to confirm your progress and catch any remaining failures.

In your final reply, list each bug you fixed and the source file it was
in.
