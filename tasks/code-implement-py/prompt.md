Implement the four functions specified in `SPEC.md` inside `textkit.py` in the
current directory. A sanity test lives under `tests/`; a fuller hidden suite is
used for grading, so follow the spec exactly (edge cases included).

Run the visible tests with:

    python3 -m unittest discover -s tests -t .

In your final reply, briefly note which functions you implemented.
