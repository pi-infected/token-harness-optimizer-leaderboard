`numkit.py` in the current directory is a small utility library with several
bugs. The test suite under `tests/` defines the correct behaviour for every
function. Make the entire suite pass:

    python3 -m unittest discover -s tests -t .

Fix `numkit.py` only — do not edit the tests. In your final reply, list the
functions you changed.
