This is the source tree of the `click` Python library.

Question: when a command-line option is not provided on the command line,
click can fall back to an environment variable — including variable names
generated automatically from a prefix configured on the context.

Explain precisely where this is implemented: name the file(s), the
function/method name(s) that resolve the environment value, and the setting
that controls automatic name generation. Maximum 15 lines, in your final
reply. Cite real identifiers from the code.
