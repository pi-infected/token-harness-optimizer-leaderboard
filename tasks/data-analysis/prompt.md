`sales.csv` in the current directory contains one year of order data
(order_id, date, region, product, units, unit_price). Revenue of an order is
units × unit_price.

Answer these questions exactly, in your final reply:

1. Which region generated the most total revenue?
2. Which product sold the most total units?
3. Which calendar month had the highest total revenue?
4. How many individual orders had a revenue strictly greater than $5,000?
   (exact count)
