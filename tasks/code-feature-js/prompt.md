Implement the feature described in `SPEC.md` in the current directory.

Follow the spec exactly — it will be checked against tests you cannot see,
covering every numbered requirement. The existing tests (`npm test`) must
keep passing.

In your final reply, summarize in a few lines how you implemented it.
