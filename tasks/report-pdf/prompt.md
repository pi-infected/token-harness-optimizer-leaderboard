`sales.csv` in the current directory contains one year of order data
(order_id, date, region, product, units, unit_price).

Produce a management report as a PDF file named `report.pdf` in the current
directory. It must contain: total revenue, revenue by region, the top region,
the top product by units sold, the month with the highest revenue, and at
least three data-backed recommendations. Use any tooling available on this
machine to build the PDF.

In your final reply, also state in plain text: the top region by revenue,
the top product by units sold, and the name of the best month.
