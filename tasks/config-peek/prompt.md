`config.toml` in the current directory is a short service configuration file.

Answer these questions exactly, in your final reply:
1. What TCP port does the server listen on?
2. What is the configured database name?
