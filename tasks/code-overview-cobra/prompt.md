This is the source tree of `cobra`, the Go CLI framework.

Write a file `ARCHITECTURE.md` at the repository root for a new contributor:

- the main components and what each is responsible for,
- which source files implement each component (cite real file names),
- how a command line goes from `Execute()` to a command's `Run` function,
- where shell completions and documentation generation live.

Be accurate and specific to THIS codebase; cite at least eight real source
files. Keep it under 120 lines.
