`access.log` in the current directory is one day of web server traffic
(combined log format).

Answer these questions exactly, in your final reply:

1. How many requests returned a 5xx status code? (exact count)
2. Which client IP made the most requests?
3. There was an error burst on one endpoint during one specific hour of the
   day: which endpoint (path), and which hour (0-23)?
